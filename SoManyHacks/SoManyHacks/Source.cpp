#include <iostream>

int main()
{
	int daHax = 0;
	while (true)
	{
		daHax += 1;
		std::cout << daHax;
		//this next bit makes it so that only one number prints at a time
		//system("cls");
	}
}